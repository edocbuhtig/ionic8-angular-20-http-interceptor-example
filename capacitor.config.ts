import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.httpinterceptorExample',
  appName: 'http-interceptor-example',
  webDir: 'www'
};

export default config;
